import express from "express";

const router = express.Router();

router.post('/agregarProducto',); // Funcional
// router.post('/Register' ,validatorParams, validator, register); // Funcional

export default router;

